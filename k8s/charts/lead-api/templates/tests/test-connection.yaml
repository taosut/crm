apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lead-api.fullname" . }}-test-connection"
  labels:
{{ include "lead-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lead-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
